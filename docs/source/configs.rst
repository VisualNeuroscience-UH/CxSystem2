Configuration files
=====================

The two main interfaces of the CxSystem are two csv files: Network & Model configuration file and Physiological configuration file, using which the CxSystem is configured.
